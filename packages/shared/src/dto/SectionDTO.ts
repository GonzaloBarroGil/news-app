// Guardian field mapping:
// id          ← id
// name        ← webTitle
// apiUrl      ← apiUrl

export interface SectionDTO {
  readonly id: string;
  readonly name: string;
  readonly apiUrl: string;
}
